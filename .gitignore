# Environment — never commit API keys
.env

# Database — persisted externally, not in repo
data/
*.db

# Python
__pycache__/
*.pyc
*.pyo
.Python
*.egg-info/
dist/
build/

# Output files — generated at runtime (ignore JSON but keep the Python package)
output/*.json

# macOS
.DS_Store

# IDE
.vscode/
.idea/
